import { FormatInServicePipe } from './format-in-service.pipe';

xdescribe('FormatInServicePipe', () => {
  it('create an instance', () => {
    const pipe = new FormatInServicePipe();
    expect(pipe).toBeTruthy();
  });
});
